[Unit]
Description=AdaptText
After=network.target

[Service]
User=thilisadunik
WorkingDirectory=/root/adapttext/api
EnvironmentFile=/root/adapttext/api/.env
ExecStart=ddtrace-run /root/adapttext/api/env/bin/flask run --host=0.0.0.0
Restart=always

[Install]
WantedBy=multi-user.target